### 问题提出

假设固定几台代理服务器，对音视频流进行透明代理转发，希望转发性能越高越好。

假设一台代理服务器负责5万路码率为1Mbps的音视频流的转发，udp包大小为800B，那么这台代理服务器的转发性能需要达到：

```
50000 x 1000000 / 8 / 800 = 800万pps
```

### 方案一：利用iptables设置转发规则，内核态转发

三台机器 A，B，C，2c4g，30万pps+100Mbps外网带宽，A运行udp客户端，B负责代理，C运行udp服务端。

#### step1：每台机器设置套接字缓冲区大小，避免流量抖动的时候丢包：

修改/etc/sysctl.conf

```
net.core.rmem_default=1000000
net.core.rmem_max=1000000
net.core.wmem_default=1000000
net.core.wmem_max=1000000
```

然后执行：

```
sudo sysctl -p
```

#### step2：机器B设置转发模式，并且设置iptables：

```
net.ipv4.ip_forward=1
net.ipv6.conf.all.forwarding=1
```

然后执行：

```shell
sudo sysctl -p
```



```shell
#!/usr/bin/bash
A_IP=10.11.7.84
B_IP=10.11.7.199
C_IP=10.11.7.239

ports=("7890" "7891" "7892" "7893" "7894" "7895" "7896" "7897" "7898" "7899" "7900")

for PORT in "${ports[@]}"
do
    echo $PORT
    sudo iptables -t nat -D PREROUTING -s  ${A_IP} -p udp --dport $PORT -j DNAT --to-destination ${C_IP}
    sudo iptables -t nat -D POSTROUTING -d ${C_IP} -p udp --dport $PORT -j SNAT --to-source  ${B_IP}

    sudo iptables -t nat -A PREROUTING -s  ${A_IP} -p udp --dport $PORT -j DNAT --to-destination ${C_IP}
    sudo iptables -t nat -A POSTROUTING -d ${C_IP} -p udp --dport $PORT -j SNAT --to-source  ${B_IP}


    sudo iptables -t nat -D POSTROUTING -s ${C_IP} -p udp --sport $PORT -j SNAT --to-source ${B_IP}
    sudo iptables -t nat -A POSTROUTING -s ${C_IP} -p udp --sport $PORT -j SNAT --to-source ${B_IP}
done

```

#### step3：在A运行udp客户端，在C运行udp服务器程序：

客户端

```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/socket.h>
#include <netinet/in.h>
#include <arpa/inet.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/socket.h>
#include <netinet/in.h>
#include <arpa/inet.h>
#include <sys/time.h>
#include <unistd.h>

//#define SERVER_IP "10.11.7.239" // server
#define SERVER_IP "10.11.7.199"  // ambassador
#define BUFFER_SIZE 1024
#define TIMEOUT_SEC 1

int main(int argc, char**argv) {
    int sockfd;
    struct sockaddr_in server_addr;
    char send_buffer[BUFFER_SIZE];
    char recv_buffer[BUFFER_SIZE];
    struct timeval timeout;
    fd_set read_fds;
    int port = 7890;


    // 创建UDP套接字
    sockfd = socket(AF_INET, SOCK_DGRAM, 0);
    if (sockfd < 0) {
        perror("socket creation failed");
        exit(EXIT_FAILURE);
    }
    int interval = 10;
    if (argc > 1)
    {
        interval = atoi(argv[1]);
    }
    if (argc > 2)
    {
        port = atoi(argv[2]);
    }

    // 设置服务器地址
    memset(&server_addr, 0, sizeof(server_addr));
    server_addr.sin_family = AF_INET;
    server_addr.sin_port = htons(port);
    if (inet_pton(AF_INET, SERVER_IP, &(server_addr.sin_addr)) <= 0) {
        perror("inet_pton failed");
        exit(EXIT_FAILURE);
    }
    memset(send_buffer, 0, sizeof(send_buffer));
    memset(send_buffer, 'A', 500);

    unsigned long sndCnt = 0, rcvCnt = 0;
    if (fork() == 0)
    {
            while (1)
            {
                    if (sendto(sockfd, send_buffer, strlen(send_buffer), 0, (struct sockaddr*)&server_addr, sizeof(server_addr)) < 0) {
                            perror("sendto failed");
                            exit(EXIT_FAILURE);
                    }
                    sndCnt++;
                    if ((sndCnt % 1000) == 0)
                    {
                            //printf("send:%lu\n",  sndCnt);
                            usleep(1000*interval);
                    }

            }

    }
#if 1
    else
    {
            //for (int i = 0; i < 10; ++i)
            //{
                //if (fork() == 0) {break;}
            //}

            while (1)
            {
                    ssize_t recv_len = recvfrom(sockfd, recv_buffer, BUFFER_SIZE, 0, NULL, NULL);
                    if (recv_len < 0) {
                            perror("recvfrom failed");
                            exit(EXIT_FAILURE);
                    }
                    rcvCnt++;
                    if ((rcvCnt % 1000) == 0)
                    {
                            //printf("recv:%lu\n",  rcvCnt);
                    }

            }
    }
#endif

    // 关闭套接字
    close(sockfd);

    return 0;
}

```

多实例脚本：

```shell
killall -9 client

./client 12 7890 &
./client 12 7891 &
./client 12 7892 &
./client 12 7893 &
./client 12 7894 &
./client 12 7895 &
./client 12 7896 &
./client 12 7897 &
./client 12 7898 &
./client 12 7899 &
./client 12 7900 &

sleep 1
ps auxw|grep client

```



服务端：

```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/socket.h>
#include <netinet/in.h>
#include <arpa/inet.h>

#define BUFFER_SIZE 1024

int main(int argc, char** argv) {
    int sockfd;
    struct sockaddr_in server_addr, client_addr;
    char buffer[BUFFER_SIZE];
    socklen_t addr_len;
    int port = 7890;
    if (argc > 1)
    {
        port = atoi(argv[1]);
    }



    // 创建UDP套接字
    sockfd = socket(AF_INET, SOCK_DGRAM, 0);
    if (sockfd < 0) {
        perror("socket creation failed");
        exit(EXIT_FAILURE);
    }

    // 设置服务器地址
    memset(&server_addr, 0, sizeof(server_addr));
    server_addr.sin_family = AF_INET;
    server_addr.sin_addr.s_addr = INADDR_ANY;
    server_addr.sin_port = htons(port);

    // 将套接字绑定到服务器地址
    if (bind(sockfd, (struct sockaddr*)&server_addr, sizeof(server_addr)) < 0) {
        perror("bind failed");
        exit(EXIT_FAILURE);
    }

    printf("UDP Echo服务器正在运行，监听端口：%d\n", port);

    for (int i = 0; i < 0; ++i)
    {
        if (fork() == 0)
        {
            break;
        }
    }

    while (1) {
        memset(buffer, 0, sizeof(buffer));
        addr_len = sizeof(client_addr);

        // 接收来自客户端的消息
        ssize_t recv_len = recvfrom(sockfd, buffer, BUFFER_SIZE, 0, (struct sockaddr*)&client_addr, &addr_len);
        if (recv_len < 0) {
            perror("recvfrom failed");
            exit(EXIT_FAILURE);
        }

        // 将接收到的消息发送回客户端
        if (sendto(sockfd, buffer, strlen(buffer), 0, (struct sockaddr*)&client_addr, addr_len) < 0) {
            perror("sendto failed");
            exit(EXIT_FAILURE);
        }


    }

    // 关闭套接字
    close(sockfd);

    return 0;
}
```

多实例脚本：

```shell
killall -9 ./echo

./echo 7890  &
./echo 7891  &
./echo 7892  &
./echo 7893  &
./echo 7894  &
./echo 7895  &
./echo 7896  &
./echo 7897  &
./echo 7898  &
./echo 7899  &
./echo 7900  &

sleep 1
ps auxw|grep echo

```



#### 测试结果，

受限于机器配置，机器B 收14万 pps 发14万pps，B的cpu利用率 10%左右。   可以猜测如果是线性的话，2c4g 双向代理可以达到140万pps每个方向，

```shell
ubuntu@VM-1-8-ubuntu:~$ netstat -su |grep 'packets sent'; sleep 10; netstat -su |grep 'packets sent'
    275276034 packets sent
    276690999 packets sent
ubuntu@VM-1-8-ubuntu:~$ bc -l
bc 1.07.1
Copyright 1991-1994, 1997, 1998, 2000, 2004, 2006, 2008, 2012-2017 Free Software Foundation, Inc.
This is free software with ABSOLUTELY NO WARRANTY.
For details type `warranty'.
276690999-275276034
1414965
```

